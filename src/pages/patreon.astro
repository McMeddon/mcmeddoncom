---
import Layout from '../layouts/Layout.astro';
/* import Card from '../components/Card.astro';
import Herobanner from '../components/herobanner.astro';
import BBcode from '../components/bbcode.astro';
import Instruction from '../components/Instruction.astro';
import McPing from '../components/McPing.astro';
import BGani from '../components/BG-animation.astro';
import BGani2 from '../components/BG-animation2.astro'; */


export interface Props {
    title: string;
}

const { title } = Astro.props;
---

<Layout title="Index">
	
	
	<main>		
		
		<h1><span class="text-gradient">Weekly Maps, environments,<br> and more since 2020</span></h1>
		
	</main>
</Layout>

<style>

/* ---------------------------------------------- */

	main {		
		padding: 1.5rem;
	}
	.wrapper{ 
	max-width: 100% !important;
	height:auto;
	display: grid;
	grid-template-columns: auto;
	font-size: large;
	font-weight: 400;
	color: #ededed;
}
.wrapper img, #demo img {
	border-radius: 25px; 
	border: 1px solid white;
	margin-bottom: .5em; 
	height:auto;
	max-width:100%;
	}

h1 {
	color:#efefef;
	font-size: 5rem;
	font-weight: 900;
	margin: 0;
}
.contentwrapper{
	display: grid;
	grid-template-columns: 1fr;
	grid-column-gap: 2%;
}
	
	.link-card-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(24ch, 1fr));
		gap: 1rem;
		padding: 0;
	}
/* ---------------------------------------------- */	



</style>
